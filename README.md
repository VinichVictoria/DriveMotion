# 🚗 DriveMotion - Система прокату автомобілів

**DriveMotion** - це сучасний мікросервіс для автоматизації процесів прокату авто, розроблений на базі архітектури Jakarta EE. Проєкт реалізує повний цикл керування автопарком, користувачами та замовленнями.

## 🛠 Стек технологій
* **Backend:** Java 17+, Jakarta EE 10 (EJB, JAX-RS, JPA)
* **Application Server:** Payara Micro
* **Database:** PostgreSQL 17
* **Build Tool:** Maven
* **Testing:** Postman, SQL Verification

## 🏗 Архітектура бази даних
Проєкт використовує реляційну модель даних, що забезпечує цілісність за допомогою зовнішніх ключів (Foreign Keys). Основні сутності:
* `users` - клієнти та адміністратори системи.
* `cars` - реєстр транспортних засобів із тарифікацією.
* `rental_orders` - історія та поточні транзакції оренди.



## 🚀 Ключові можливості
- **Управління автопарком:** Додавання, редагування та фільтрація доступних авто за ціною та статусом.
- **RESTful API:** Ендпоїнти для реєстрації користувачів та оформлення замовлень у форматі JSON.
- **Контроль цілісності:** Валідація даних на рівні бази даних та бізнес-логіки додатка.

## 🧪 Приклади тестування
Для верифікації системи було проведено покрокове тестування через SQL-інтерфейс:
```sql
-- Перевірка активних замовлень
SELECT u.full_name, c.model, o.status 
FROM rental_orders o
JOIN users u ON o.user_id = u.id
JOIN cars c ON o.car_id = c.id;
